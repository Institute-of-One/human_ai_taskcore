# python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
.pytest_cache/

# review evidence — NEVER commit (IORN-002 protocol)
review_evidence/

# papers retrieved for the H2 external validation — NEVER commit (copyright).
# What we extract from them lives in data/h2_studies.json with page and table
# provenance, which is the part that has to be reproducible.
data/papers/

# paper build artifacts and submission-only files
paper/*.tmp
paper/.~lock*
paper/susy_*.txt
paper/response_to_reviewers_*
paper/cover_letter_*
paper/rev_*.jpg

# OS noise
.DS_Store
Thumbs.db
desktop.ini
